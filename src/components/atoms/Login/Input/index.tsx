import React, { FC, ReactNode } from 'react'

interface ILoginInputProps {
    placeholder: string
}

export const LoginInput = ({ placeholder, ...rest }: ILoginInputProps) => {
    return (
        <></>
    )
}
